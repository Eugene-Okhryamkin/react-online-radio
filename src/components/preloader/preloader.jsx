import React from "react";
import "./preloader.sass";

const Preloader = () => {
    return(
        <div id="preloader">
            <div id="inside"></div>
        </div>
    );
};

export default Preloader;